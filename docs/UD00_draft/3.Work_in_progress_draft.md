
# 3. Este punto no se debería mostrar en despliegue.

Se puede usar el sufijo '_draft' en el nombre del fichero para que no sea mostrado en despliegue.

## Estado

Esta sección se muestra en `serve` pero no en build.